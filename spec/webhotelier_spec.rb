require 'spec_helper'

describe Webhotelier do
  it 'has a version number' do
    expect(Webhotelier::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
